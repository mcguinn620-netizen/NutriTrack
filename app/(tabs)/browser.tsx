/**
 * browser.tsx — stub file kept for Expo Router route consistency.
 * The Browser tab has been removed from the tab bar via _layout.tsx (href: null).
 * This file intentionally avoids platform-specific imports so it bundles safely on all targets.
 */
import React from 'react';
import { View } from 'react-native';

export default function BrowserTab() {
  return <View />;
}
